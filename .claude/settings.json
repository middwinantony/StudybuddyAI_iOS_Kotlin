{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "git diff --quiet && git diff --cached --quiet || (git add -A && git commit -m \"Auto-save $(date '+%Y-%m-%d %H:%M:%S')\") 2>/dev/null || true",
            "statusMessage": "Auto-saving changes..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git diff --quiet && git diff --cached --quiet || (git add -A && git commit -m \"Auto-save $(date '+%Y-%m-%d %H:%M:%S')\") 2>/dev/null || true",
            "statusMessage": "Auto-saving pending changes..."
          }
        ]
      }
    ]
  }
}
